// Hover and keyboard focus both select the linked project preview.
const projects=[{name:'מעונות כחממה',file:'final',caption:'FIG. 01 / FINAL PROJECT'},{name:'אשכול גנים',file:'kindergarten',caption:'FIG. 02 / KINDERGARTEN'},{name:'מעונות סטודנטים',file:'housing',caption:'FIG. 03 / STUDENT HOUSING'}];
const rows=[...document.querySelectorAll('.project-row')];
function selectProject(i){const p=projects[i];rows.forEach((row,n)=>row.classList.toggle('active',n===i));document.querySelectorAll('.preview-image').forEach((img,n)=>img.classList.toggle('shown',n===i));const link=document.getElementById('project-preview');link.href='assets/'+p.file+'.pdf';link.setAttribute('aria-label','פתיחת פרויקט '+p.name+', PDF בלשונית חדשה');document.getElementById('preview-name').textContent=p.name;document.getElementById('preview-number').textContent=p.caption;}
rows.forEach((row,i)=>{row.addEventListener('pointerenter',e=>{if(e.pointerType!=='touch')selectProject(i)});row.addEventListener('focus',()=>selectProject(i));});
// Content stays visible if JavaScript or IntersectionObserver is unavailable.
if('IntersectionObserver' in window){document.documentElement.classList.add('js');const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));}
let queued=false;const progress=document.querySelector('.progress');function updateProgress(){const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max>0?scrollY/max*100:0)+'%';queued=false;}addEventListener('scroll',()=>{if(!queued){queued=true;requestAnimationFrame(updateProgress)}},{passive:true});addEventListener('resize',updateProgress);updateProgress();
